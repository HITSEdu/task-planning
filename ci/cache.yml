.cache:
  cache:
    key:
      files:
        - pnpm-lock.yaml
    paths:
      - node_modules/
    policy: pull

.cache.write:
  cache:
    key:
      files:
        - pnpm-lock.yaml
    paths:
      - node_modules/
    policy: pull-push
